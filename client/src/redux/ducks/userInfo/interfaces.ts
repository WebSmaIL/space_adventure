export interface IState {
    id: string,
    email: string,
    login: string,
    isAuthorize: boolean,
    balance: number,
    level: number,
};

export interface ISetUser {
    id: string,
    email: string,
    login: string,
    balance: number,
    level: number,
};